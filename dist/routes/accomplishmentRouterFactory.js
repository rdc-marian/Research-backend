"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express = require("express");
const { authenticate } = require("../middleware/auth");
const { upload } = require("../middleware/upload");
/**
 * Reusable router factory for all portfolio accomplishment categories.
 * Wires the standard CRUD endpoints and file upload middleware.
 *
 * @param {object} controller - The controller instance generated by createAccomplishmentController
 */
const createAccomplishmentRouter = (controller) => {
    const router = express.Router();
    router.use(authenticate);
    router
        .route("/")
        .get(controller.getAll)
        .post(upload.single("file"), controller.create);
    router
        .route("/:id")
        .get(controller.getOne)
        .patch(upload.single("file"), controller.update)
        .delete(controller.delete);
    router.patch("/:id/status", controller.updateStatus);
    return router;
};
module.exports = createAccomplishmentRouter;
